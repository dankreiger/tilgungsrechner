# loans-graphql

This is a GQL api for the `loans` domain.

It can be used in any app under `apps/loans` or `libs/loans`.

## TODO: establish the importing rules with nx project tags.

## Running unit tests

Run `nx test loans-graphql` to execute the unit tests via [Jest](https://jestjs.io).

## Running lint

Run `nx lint loans-graphql` to execute the lint via [ESLint](https://eslint.org/).
